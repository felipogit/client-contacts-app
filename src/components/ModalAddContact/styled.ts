import styled from "styled-components";

export const StyledForm = styled.form`
        display: flex;
        flex-direction: column;
        gap: 20px;
        height: auto;
        width: 100%;
        max-width: 300px;
        
        padding: 10px;

        input{
            height: 35px;
        }

        button{
            margin-top: 20px;
            height: 35px;
        }


`